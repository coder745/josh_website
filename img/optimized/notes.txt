The SVG images in this folder were optimized with the node 'svgo' command below:
svgo --pretty git.svg git_optimized.svg

I Moved them into this folder and then took the code from these files and implemented into the html page via Inline SVG Sprites.
